<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Necessário para o ffmpeg acessar dispositivos AVFoundation
       (câmera, placa de captura HDMI exposta como câmera). Sem isso o
       sistema bloqueia silenciosamente. -->
  <key>com.apple.security.device.camera</key>
  <true/>

  <!-- PyInstaller usa JIT/dlopen para carregar a stdlib em runtime.
       Necessário em apps notarizados. Em apps não-notarizados também
       não machuca. -->
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
